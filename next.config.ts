import type { NextConfig } from "next";

// Template next.config — the Generate-Site pipeline may inject per-client
// redirects (for SEO equity preservation when migrating from old domains)
// by editing this file. The default template ships with no redirects.
const nextConfig: NextConfig = {};

export default nextConfig;
